# Story 8.3: Grid Import Integration in Grids Page

## Status

Done

## Story

**As a** teacher,
**I want** an "Import CSV" button on the grids page alongside the existing "Create New Grid" button,
**so that** I can easily access the import feature from the same place I manage my grids.

## Acceptance Criteria

1. An "Importer CSV" button appears next to the existing "Create New Grid" button on `app/grids/page.tsx`
2. Clicking the button opens the `GridCSVImporter` modal from Story 8.2
3. After a successful import, the new grid appears immediately in the grid listing (via existing `useLiveQuery`)
4. The "Create New Grid" button and link to `/grids/new` remain unchanged
5. The empty state (no grids) also shows the import option as an alternative to creating manually
6. Both buttons are visually balanced and tablet-friendly (min 44px touch targets)
7. The existing grid deletion functionality is not affected

## Tasks / Subtasks

- [x] Task 1: Add import state management to `app/grids/page.tsx` (AC: 2)
  - [x] Add `const [showImporter, setShowImporter] = useState(false)` state
  - [x] Import `GridCSVImporter` component
- [x] Task 2: Add "Importer CSV" button in header bar (AC: 1, 6)
  - [x] Place button next to existing "Create New Grid" Link
  - [x] Use `Upload` icon from lucide-react
  - [x] Style: `bg-emerald-600 text-white hover:bg-emerald-700` to visually differentiate from the blue "Create" button
  - [x] `onClick={() => setShowImporter(true)}`
  - [x] Match height `h-12` of existing button
- [x] Task 3: Render `GridCSVImporter` modal conditionally (AC: 2, 3)
  - [x] `{showImporter && <GridCSVImporter onClose={() => setShowImporter(false)} />}`
  - [x] Grid listing auto-refreshes via existing `useLiveQuery` — no additional wiring needed
- [x] Task 4: Update empty state with import option (AC: 5)
  - [x] Add an "ou Importer depuis CSV" button/link below the existing "Create Grid" button in the empty state section
  - [x] Style as secondary action: `bg-emerald-50 text-emerald-700 border border-emerald-200`
- [x] Task 5: Verify no regressions (AC: 4, 7)
  - [x] Verify "Create New Grid" link still navigates to `/grids/new`
  - [x] Verify grid deletion still works
  - [x] Verify `GridEditor` is unmodified
  - [x] Verify existing grids display correctly

## Dev Notes

### Current Page Structure

`app/grids/page.tsx` currently has:

```
Header bar:
  - H1 "Grading Grids"
  - Link "/grids/new" → "Create New Grid" (blue-600, flex items-center gap-2, h-12)

Content:
  - Empty state (when grids.length === 0): icon + text + "Create Grid" link
  - Grid cards (when grids exist): grid of cards with name, version, competency/indicator counts
```

[Source: app/grids/page.tsx]

### Integration Approach

Minimal changes to `page.tsx`:
1. Add one `useState` for modal visibility
2. Add one button in the header `flex` container
3. Add one conditional render for the modal
4. Add one button in empty state

The `useLiveQuery(() => db.grids.toArray())` already powers the listing — any `db.grids.add()` from the importer will automatically trigger a re-render. No additional plumbing needed.

[Source: app/grids/page.tsx - useLiveQuery pattern]

### File Changes

- **Modified:** `app/grids/page.tsx` (add import button + modal toggle)
- **No other files modified**

### Dependencies

- Story 8.2 (GridCSVImporter component) must be completed first
- No new npm packages required

### Testing

- Manual: open grids page, click "Importer CSV", upload valid file, import, verify grid appears in list
- Manual: verify "Create New Grid" still works as before
- Manual: verify delete grid still works
- Manual: test on tablet viewport (768px) — both buttons should be accessible
- Manual: test empty state shows both create and import options

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6

### File List
| File | Status | Description |
|------|--------|-------------|
| `app/grids/page.tsx` | MODIFIED | Added useState, Upload icon, GridCSVImporter import, "Importer CSV" button in header + empty state, modal conditional render |

### Completion Notes
- Minimal changes as planned: 3 imports added, 1 state, 1 button in header, 1 button in empty state, 1 modal render
- Header buttons wrapped in flex container with gap-3 for visual balance
- Empty state uses secondary style (emerald-50 bg with border) as specified
- All touch targets h-12 (48px)
- GridEditor.tsx untouched — verified no regressions
- useLiveQuery handles auto-refresh after import — no extra wiring
- TypeScript clean, lint clean

### Debug Log References
None.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-09 | 2.0 | Implementation complete — all tasks done | Dev Agent (James) |
| 2026-03-09 | 1.0 | Initial story draft | SM Agent (Bob) |
